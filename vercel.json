{
  "version": 2,
  "builds": [
    {
      "src": "server.js",  // Change this to your actual server file
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server.js"  // Ensure this points to server.js
    }
  ]
}
